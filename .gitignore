*.iml
.gradle/
.kotlin/
local.properties
.idea/
.DS_Store
build/
/captures
.externalNativeBuild
.cxx
*.jks
*.keystore

# Expo
.expo/
node_modules/

# Firebase service account keys — never commit
*service-account*.json
*adminsdk*.json
*firebase-adminsdk*.json

# Backend sensitive dev config (contains real JWT keys / credentials)
**/application-dev.yml

# Local scratch file with live deploy secrets — never commit
RENDER_DEPLOY_SECRETS.md
RENDER_SG_BACKEND_ENV.md
