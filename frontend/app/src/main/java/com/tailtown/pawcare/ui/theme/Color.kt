package com.tailtown.pawcare.ui.theme

import androidx.compose.ui.graphics.Color

// Brand
val Coral = Color(0xFFD4537E)       // primary CTA, links, saves
val CoralSoft = Color(0xFFFBEAF0)   // hero plates, highlights

// Ink
val Ink900 = Color(0xFF1A1A1A)      // primary text, dark CTA
val Ink500 = Color(0xFF5A5A5A)      // supporting copy

// Canvas
val Bone = Color(0xFFF5F2EC)        // page background
val BoneWarm = Color(0xFFF5EACF)    // warm hero (permissions screen)

// Semantic
val Teal600 = Color(0xFF0F6E56)     // healthy, paid, delivered
val Amber600 = Color(0xFF854F0B)    // due, expiring, attention
val Blue600 = Color(0xFF185FA5)     // selected, default address

// Utility
val White = Color(0xFFFFFFFF)
val Hairline = Color(0xFFE0DBD5)    // borders, dividers (1px resting)
